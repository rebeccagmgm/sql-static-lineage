{
  "name": "sql-static-lineage-l1",
  "version": "0.1.0",
  "private": true,
  "description": "Evidence-bound SQL Static Lineage L1 vertical slice.",
  "engines": {
    "node": ">=20.11"
  },
  "keywords": [
    "sql",
    "static-lineage",
    "machine-facts",
    "databricks"
  ],
  "scripts": {
    "prepare:deps": "node scripts/ensure-dependencies.mjs",
    "prebuild": "npm run prepare:deps",
    "build": "tsc -p tsconfig.build.json",
    "pretypecheck": "npm run prepare:deps",
    "typecheck": "tsc -p tsconfig.json",
    "pretest": "npm run prepare:deps",
    "test": "vitest run tests/machine-facts.test.ts tests/plan-adapter-regression.test.ts tests/task-inspection.test.ts tests/input-pack.test.ts tests/repair-stored-input-packs.test.ts tests/reconcile-one-hop.test.ts tests/producer-index.test.ts tests/reconcile-multi-hop.test.ts tests/task-partition-evidence.test.ts",
    "pretest:engine": "npm run prepare:deps",
    "test:engine": "vitest run tests/plan-adapter-regression.test.ts",
    "pretest:machine-facts": "npm run prepare:deps",
    "test:machine-facts": "vitest run tests/machine-facts.test.ts",
    "pretest:consumer": "npm run prepare:deps",
    "test:consumer": "vitest run tests/task-inspection.test.ts",
    "test:input-pack": "vitest run tests/input-pack.test.ts",
    "test:reconcile-one-hop": "vitest run tests/reconcile-one-hop.test.ts",
    "test:producer-index": "vitest run tests/producer-index.test.ts",
    "test:partition-evidence": "vitest run tests/task-partition-evidence.test.ts",
    "test:reconcile-multi-hop": "vitest run tests/reconcile-multi-hop.test.ts",
    "reconcile-one-hop": "tsx scripts/reconcile/reconcile-one-hop.ts",
    "reconcile-multi-hop": "tsx scripts/reconcile/reconcile-multi-hop.ts",
    "producer-index": "tsx scripts/reconcile/producer-index.ts",
    "producer-index:update": "tsx scripts/reconcile/producer-index.ts --update",
    "input-pack:tasks": "tsx scripts/input/collect-task-input-pack.ts",
    "input-pack:rebuild-partitions": "tsx scripts/input/rebuild-task-partition-evidence.ts",
    "input-pack:repair-stored": "tsx scripts/input/repair-stored-input-packs.ts",
    "repair:missing-table-packs": "tsx scripts/input/repair-missing-table-packs.ts",
    "preinspect": "npm run prepare:deps",
    "inspect": "tsx scripts/query/task-inspection.ts",
    "preformat:check": "npm run prepare:deps",
    "format:check": "prettier --check package.json README.md docs tests/gold"
  },
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@types/node": "^26.1.1",
    "prettier": "^3.8.3",
    "tsx": "^4.22.4",
    "typescript": "^7.0.2",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "antlr4ng": "^3.0.16"
  }
}
