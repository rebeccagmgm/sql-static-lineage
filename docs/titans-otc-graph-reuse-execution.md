# Titans → OTC 图谱共享执行清单

设计依据：[titans-otc-graph-reuse-design.md](titans-otc-graph-reuse-design.md)。

## 授权和工作区

用户要求先写完整方案、细化步骤，高把握部分可直接开始。补充要求：Facts/投影位于 sql-static-lineage-data 独立子目录，完整检查集中归并发布阶段，data/evidence/inputPack 等路径统一配置。

本轮开始前工作区已有采集、Facts、Union 1.3.0 兼容和文档改动；保留这些改动，不重置、不为本方案改写既有业务语义。Facts 合同 1.3.0、adapter 1.3.9、status 1.0.0 已核对。

## A：统一配置与跨图共享（高把握，先执行）

- [x] A1 写入默认 JSON 配置和公共 resolver，明确定义 data/evidence/inputPack/Facts/projection/graph/writer-catalog 路径、profile 与优先级。
- [x] A2 先写配置测试：不同 cwd、相对路径、CLI/env 覆盖、非法配置及现有目录默认值。
- [x] A3 Facts CLI、投影 CLI 接入配置；原有显式参数和程序 API 保持兼容。
- [x] A4 投影 CLI 将缓存写到共享 projectionRoot，图 outputRoot 只存批次清单；不同图引用同一投影。
- [x] A5 字段查询的默认根路径/旧案例位置改由配置解析，明确其仍待 B 阶段切到纯读取发布批次。
- [x] A6 过渡期缓存键纳入调度内容指纹，保留无调度旧调用的兼容行为。
- [x] A7 验证两次不同图目录使用同一任务，第二次 cache hit；调度关系变化可使相关投影失效。
- [x] A8 执行针对性测试、typecheck、改动检查；记录实际结果、未解决问题和代码审查结论。

A 的完成不是 B/C 或整批上线完成。A 不复制全部历史实验目录、不启动全量 Facts 生成、不改变接续候选资格。

## B：归并发布校验与纯读取查询

- [x] B1 固定共享投影的不可变版本引用，旧批次不随当前缓存覆盖改变。
- [x] B2 归并/发布一次校验 Facts/Pack 指纹、投影与接续索引引用；复用同一 Facts Reader，避免重复准备。
- [x] B3 查询按批次读取所需投影/关系树，禁止隐式调用投影生成器；必要加工证据按固定版本定位。
- [x] B4 对缺少投影给出具体任务缺口，不把旧 Greek 索引自动当作 Titans/OTC 批次。
- [x] B5 统一任务并集与 data-graph 查询入口的输入选择，保留原有已发布图目录查询。
- [x] B6 对新旧查询结果做同输入对照，确认仅加载方式改变，值/控制/边界没有丢失。

B 开始前先检查既有 batch/index 的版本关系。缓存核验不能直接删除后就宣称发布已经校验；必须先具备真实的发布验证入口。

## C：接续效率与加工消费

- [x] C1 为并集读取、写入、表、上下游建立一次性查找索引，替代每个 read 的全并集搜索。
- [x] C2 定点验证调度共同供数与竞争 writer：四产品分区不能因多个父任务统一阻断。
- [x] C3 加工查询返回公式、过滤条件、Join 键、分配与聚合上下文及 SQL 定位。
- [x] C4 完成客户每日本金、TRS 同源异名、销售日报窗口差异的真实查询验证。
- [x] C5 小批耗时/内存评估后，准备 1,674 Facts 对应投影及接续，并保留 3,615 工作集中的调度上下文。
- [x] C6 报告首建、首查、复查、单任务更新的耗时与覆盖结果，不外推单样本倍数。

## 验收矩阵

| 检查 | 通过条件                                                              |
| ---- | --------------------------------------------------------------------- |
| 目录 | Facts 在 field-facts；共享投影在 task-projections；图清单引用共享路径 |
| 配置 | 更换配置即可定位另一套材料；相对路径与 cwd 无隐含耦合                 |
| 复用 | 同一输入分别建立两个图批次，投影生成一次                              |
| 失效 | Pack/Facts/调度有效内容变化能更新对应产物                             |
| 查询 | B 完成后，查询期投影生成次数和 Pack 全目录扫描次数均为零              |
| 版本 | 当前合同、adapter、status 各自正确；跨批查询不混入不同来源版本        |
| 业务 | 能解释本金来源及加工差异；多产品分工不误判成重复来源                  |

## 本轮执行记录

2026-09-05：A 阶段已落地。先补配置及跨图共享测试并确认失败，再实现公共路径解析、三个入口接入和共享缓存。

- `npm run test:workspace-paths`：3 个文件、24 项通过。覆盖路径优先级、不同 cwd、既有环境变量、两个图复用同一投影、调度内容变更后的缓存更新。
- `npm run typecheck`：未通过。既有 `tests/fill-hive-task-sql-cache.test.ts:1031-1033` 直接从 HIT/MISS 联合类型读取 `querySql`，报 3 处 TS2339；本轮未改该文件，不将这一结果写成类型检查通过。
- 代码审查：共享清单仍包含投影路径与内容哈希；旧程序调用未给 `projectionRoot` 时保留原目录行为；Union envelope 可接受新增的可选调度指纹；旧 Greek 验收显式使用 `greek-legacy` 配置，避免切换默认批次后静默跳过。
- `git diff --check` 通过。保留本轮前的其他采集、Facts 和 Union 改动。

本轮没有运行 1,674 任务全量生产。共享缓存仍执行现有 Facts/Pack 核验；字段查询仍可能生成局部投影。消除这部分开销属于 B，不能把 A 的跨图复用测试视为查询加速已完成。A 的任务路径保存当前缓存，固定历史批次的不可变版本引用也由 B 实现。

上述为 A 阶段历史记录。2026-09-05 用户进一步授权完成全链路并建设、验收 3,615 任务图谱，本次继续实施 B/C 和以下 D/E。以实际产物和业务查询验收，不以代码存在代替完成。

## D：Neo4j 发布、增量与渐进消费

- [x] D1 复用本机 Neo4j，连接参数进入配置，密码只引用环境变量指定的文件。
- [x] D2 任务投影保存不可变版本；图按任务内容哈希增量发布，删除被替换任务拥有的旧关系，保留共享实体。
- [x] D3 建图时计算接续；字段路径保留 write/read occurrence 和列身份，直接来源与控制条件分开。
- [x] D4 原生查询按索引入口读取局部子图，提供任务/表概览、字段上下游、加工详情三层消费。
- [x] D5 验证不变批次零重建、单任务更新、重复发布幂等，以及查询期间零投影生成。

## E：3,615 任务真实建设和业务验收

- [x] E1 清点全量任务类型、Facts、投影覆盖，区分字段可分析任务与调度/搬运上下文。
- [x] E2 运行唯一全量构建进程，记录投影、并集、接续、入库的数量、时间、内存及失败明细。
- [x] E3 验证 T98 四产品分区共同供数、本金公式、TRS 同源分支与销售加工差异。
- [x] E4 提供可打开的调查页面和 CLI/API 查询入口，记录实际首查/复查时间及返回规模。
- [x] E5 输出完整验收报告；任何未完成范围按实际缺口列明。

构建层：Facts → 共享任务投影 → 直接关系归并与接续 → 增量发布。消费层：查询数据库已有邻接关系，按选择展开字段和加工细节；SQL 原文留在证据文件中。渐进加载控制查询量，存储去重依靠稳定实体和不保存重复传递闭包。

## 本次完整执行结果

2026-09-05：已完成 B/C/D/E 的构建与消费实现，并以 3,615 任务固定批次验收。当前字段投影 2,258，调度层 1,177，材料/Schema 缺口 180。没有将缺口填成成功字段血缘。完整结果见 [titans-otc-graph-acceptance.md](titans-otc-graph-acceptance.md)。

B3/B5 的正式入口为 Agent CLI；旧 Greek 调查 harness 保留兼容行为，不作为本批 Agent 消费入口。C3/C4 通过加工查询验证公式、过滤、分配等上下文，复杂 index_val 的字段因果闭包仍按实际投影覆盖记录，不声称全部解决。

C5 原计划 1,674 Facts 是早期快照；使用最终落盘材料后为 2,258 PROJECTED。E1/E5 包含逐任务缺口清单，建设完成不等于每个调度任务都有字段 SQL。D5 的无变化复用、增量替换、删除、共享实体和回滚均已实测。
