# 缺 writer 范围：补图修复复核（2026-09-10）

状态：**补入批次已生成并核验，尚未发布到当前运行图。**

## 结论

原执行记录把首批任务判为 PLATFORM_CONFIG_ONLY 不准确。562 个配置目标候选中，439 个已有带 write_observation_id 的 Facts 写入记录，覆盖冻结范围全部 362 张表。缺口的直接原因是这些生产任务未纳入图批次。无需为了本次补入而等待 Horae 或批量重采。

这 439 个候选全部通过现有消费者的 bundle 校验；任务元数据、源 SQL、目标表元数据及 DDL 与 Facts 引用一致，439 个投影均为 PROJECTED。旧版 1.3.0 Facts 的读取状态是 LEGACY_NOT_L1，现有投影兼容路径保留 PROVISIONAL_LEGACY 标记；该状态名称不等同于没有写入证据，也不要求为本次补图强制升级 Facts。接续确认按当前发布器的身份、输出资格、写入和分区条件判定，不代表运行成功或数据到达。

## 冻结的 1,088 个读取位置

| 补入后状态 | 数量 | 含义 |
| --- | ---: | --- |
| 完整确认 | 578 | 所有保留的非不相交候选均满足现有接续确认条件 |
| 仅候选 | 472 | 有 writer，仍存在分区未知或非字面量等限制 |
| 部分确认 | 7 | 既有确认候选，也有尚未确认的候选 |
| 全部分区不匹配 | 31 | 已知候选的写分区与读范围不相交，不强行连通 |
| 没有候选 / 读取丢失 | 0 / 0 | 冻结读位置完整保留 |

本次解决的是缺 writer 的覆盖问题。剩余 510 个位置不能说成已完整接通。新增生产任务自身还会带入上游读取，全图缺口总数不能与冻结 1,088 个位置直接相减。

## 保护与核验

- 基线运行版本：`96f927c8fba1edfa0a3bc2c8a4570de99adf83e19455da7bcbd44ec5ac2c7cd0`。
- 原 7,039 个任务条目原样保留，新增 439 个，总计 7,478 个。
- 原 9,379 个完整确认读取全部保留，新增完整确认 578 个；读取回退 0，原任务覆盖回退 0。
- 未修改源 Evidence、Input Pack、Facts、身份/分区解析代码、writer 正式目录或当前运行图。仅重建独立投影与补入批次；writer 目录使用 SQLite 一致性快照进行核验。
- 没有将任务配置目标直接提升为写入事实；入选任务必须已有目标表对应的 write_observation_id，并通过现有读取与投影验证。

## 可复核交付

- [逐读取结果 CSV](repair-read-results-20260910.csv)：交付主键仍是 consumerTaskId + readOccurrenceId + table。
- [发布交接 JSON](repair-handoff-20260910.json)：基线、补入任务清单、批次、接续索引和校验文件的确切路径。
- [完整核验结果](E:/02_area/股衍数据-数据cookbook/sql-static-lineage/.evidence-cache/writer-gap-repair-20260910/prepublish-audit.json)。
- [修复批次清单](E:/02_area/股衍数据-数据cookbook/sql-static-lineage/.evidence-cache/writer-gap-repair-20260910/graphs/repair/batches/a7fa1f30873ae0f4a3001edfb37105640b33c6c52037ca2550a21eb872b82c15/batch-manifest.json)。

原交接 README 要求“不要自行发布图”，因此本次停在可审阅的补入批次。获准发布后，须先确认 current 仍等于上述基线并重新核对校验门禁；基线变化时先合并新基线、重跑比较，再发布。不得直接覆盖正式 prepared/current 文件。
