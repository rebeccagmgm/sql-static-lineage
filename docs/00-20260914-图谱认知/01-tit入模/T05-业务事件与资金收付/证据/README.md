# T05 第一批证据说明

读取日期：2026-09-16。正文和取数 SQL 服务业务理解；本目录保存复核依据，不能将其中生产写入 SQL 当成学习取数脚本执行。

## 来源与范围

| 材料 | 作用与时间边界 |
| --- | --- |
| 用户指定 Excel：`sql-static-lineage-data/knowledge/图谱统计/T01-T06-TIT入模消费与学习导读.xlsx` | 读取“T05事件与业务处理”18 个分区条目、15 张模型表及消费索引；属已有整理快照 |
| 已发布图谱 | 版本 `9dbe562350452a41ae13c1b95f260a631fce9d2f9b8dceeba43e79dbf65f7c02`；22 个清单内入模任务均 PROJECTED，106205 未收录 |
| 图谱 processing 的 SQL 分页 | 保留第一批 9 个入模任务、消费者 228195/181556 和数量核对 188777；4 个任务另读 create 段以取得真正源投影 |
| 本地表字段目录 | `table-metadata-4877b15fb9a9accd-e5c48df0-e117-4469-8eb4-e62d3568950b.sqlite`，保留各表采集时间、限定表名、字段类型及注释 |
| 本地资产目录 | `CD375/CD758/CD498/CD1531/CD1759` 原码、含义、来源、备注、日期；完整性仍为未核验，不因有同名字段就认定同域 |
| 已有源字典 CSV | `D_CFG_DICTIONARY_DESC_合并_中英文字段.csv`，业务日 2026-09-14；摘录 TransferType、TRANSFER_STATUS、FeeType。TRS_EVENT_TYPE/TRS_EVENT_STATUS 未命中 |

`SQL索引.json` 区分图谱返回的 `graphSqlSha256` 与实际导出文本的 `exportSha256`；分页文本的行尾/表示形式可能不同，不把两者默认当作字节一致。`file` 指向本地供阅读的文件，`lines` 是图谱报告行数。SQL 中具体日期多为缓存任务当次展开值，不是本次查询日期，也不是建议导出业务日。

## 本轮确认与未确认

- 已确认第一批字段投影、身份表达、Join、过滤、更新/删除和关系有效期规则；模型状态及源状态字典的适用边界分别列出。
- 已确认 228195 费用分支取 T03 `Fee_Amt`，支付日取计划 `Paid_Date`；其费用源条件为 `D_TRD_FEE`，与计划入模辅助 `F_TRD_FEE` 有别。
- 已确认两份 Transfer 源投影没有 `BUSI_DATE` 过滤；尚未检查源分区实际留存及运行结果，不直接判定数据已重复。
- 未收到本轮 T05 业务样本，未验证金额关系、唯一性、匹配率、到账、邮件送达或业务验收。
- 第二、三批本轮只有范围/图谱收录核对和存量业务摘要，不冒充已逐条重读其全部 SQL。

取数脚本已按本地字段快照检查表、字段和 CTE 绑定，并在内存 SQLite 注册聚合函数占位后完成九段 `EXPLAIN QUERY PLAN`；已检查每段只读且限制 500 行、文档相对链接可达。该检查不执行业务数据，也不替代目标 Presto/Trino 平台的语法、权限、性能和结果验证。

## 阅读入口

返回[全貌](00-20260914-图谱认知/01-tit入模/T05-业务事件与资金收付/00%20事件与业务处理全貌.md)。需要导出时仅使用[第一批取数 SQL](../../../../attachments/README/IMG-README-20260918111629837.sql)：9 段均为只读 SELECT/CTE，每段最多 500 行，不执行本目录中的 INSERT、CREATE、DROP。
