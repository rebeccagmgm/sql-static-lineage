# 118141剩余整理：完成范围与保留边界

2026-09-22，落实已确认的六项收尾计划。范围仅为118141及解释它必需的直接上游；未扩到其他收入、规模或报送任务。阅读从[118141首页](../销售与经营分析/118141_交叉销售收入/README.md)开始，本页是维护记录，不是新增阅读前置。

## 六项工作的交付

| 工作 | 已完成内容 |
| --- | --- |
| 修复入口 | 恢复107491空主脚本及133057空生产SQL；基准空展示文件改为准确导航；修正合约主信息manifest、四个入口及公共视图检查的旧路径，重新生成对应完整SQL |
| 经营系数 | 价差增加两张T99的职责／键／日期对照；基准沿OPT_DEMO说明从OIS配置到日收入；成本以K1贯通配置生产、期初日匹配和原始收入495／490 |
| 普通互换 | 新增09说明：两条本金来源、减仓后的100／80／50、四种年化绝对组合、提前终止、分配和日报 |
| AIRBAG及关联保证金 | 新增10说明：特殊分支优先级、字段实际来源、168与159的区别、关联LONG_HOLD_SWAP置零及NULL／除零／日期边界 |
| 金仕达 | 新增11说明：两份确认书、金额延续与当天交易值、原始收入1,500／3,000、同日系统费用分摊及各自累计 |
| 收拢与核对 | 原始收入CASE由主线02引用02.1特殊类型、02.2金仕达、02.3期权、02.4互换，四份规则均实际参与组装；展示表达式在01；三个旧表达式文件只作导航，五个真正被include的参数JOIN保留；首页、旧规则索引、公共入口与参数对照同步 |

没有增加业务子文件夹。三份新说明与四份业务规则SQL平铺在118141目录中；复杂加工继续由已有阶段模块承接。02保留CASE及优先级，四份规则保留WHEN/THEN，不把相互排斥的分支改成四张需重新连接的表；输入、参数、调整、分配和输出不按产品重复复制。

## 如何核对

- 合约主信息四分支：真实入口展开后，对照各自冻结SQL；107491恢复入口后，反向还原查询对照原始23个输出字段及全部语法token。
- 118141：将阅读版三个CTE还原为原嵌套，保持整个冻结SELECT及92列输出顺序；生成文件与当前include树一致。
- 参数专题：生产、匹配继续对照原证据；展示／收入检查改为读取118141实际维护位置，没有删掉旧公式断言来迁就导航文件。
- 构造算例：读取实际输入SELECT、调整、分配累计、最终输出；覆盖普通互换四种组合、提前终止、零／NULL回退、AIRBAG特殊优先级及保证金分母、金仕达多合约系统费用分摊与零／NULL分母。最终金额字符串先检查非空和有效数值，再比较，不能把NULL或空串转换成0蒙混通过。
- 四个业务规则模块各被引用一次，顺序与冻结CASE一致；修改任何一个片段都会传递到主查询并被原文对照发现。参数专题测试使用同一个renderer展开实际规则，不绕过新模块读取旧副本。
- 联合运行公共加工六专题和118141现有检查文件，并检查本范围Markdown本地链接和空文件；具体核验结果见文末。检查数量只是维护留痕，不代替可读性验收。

复核命令（仓库根目录；先执行 `npm run prepare:deps`）：

```powershell
$area = 'docs/00-20260914-图谱认知/第二模块：报送、报表等复杂消费'
$checks = @(Get-ChildItem "$area/公共加工" -Filter '*.test.mjs' -Recurse -File |
    ForEach-Object FullName)
$checks += (Resolve-Path "$area/销售与经营分析/118141_交叉销售收入/checks.test.mjs").Path
node --test @checks
```

这些是离线静态对照与内存构造案例；参数日期展开、String、UDF及除零在SQLite中有明确适配，不冒充Hive运行。没有执行生产查询、修改调度、创建生产视图或改动计算口径。

## 仍未证明的，不以“完成整理”掩盖

| 边界 | 当前能确认什么／不能确认什么 |
| --- | --- |
| 固定系数与业务制度 | 能解释1.06、0.3、0.94、0.5和系统费用常数在表达式中的位置；不能仅凭SQL证明税务、分成或费用制度依据 |
| 源字段单位 | 能追到金仕达occupy_cost以及保证金比例、费率来源；不能推定维护界面单位、全部实际币种与经济含义 |
| 实际数据与唯一性 | 保留重复匹配、同日窗口和缺失传播；构造样例不能证明生产没有重复或异常 |
| 历史重算与支付 | 说明提前终止会改变哪些公式和上游本金；没有证明历史分区已经重算，也没有证明收入已经支付 |

上述属于数据或制度证据边界，不是继续拆更多文件就能解决。本次六项整理任务已交付；后续若要核验这些边界，应另明确真实数据范围或业务依据。

## 最终核验结果

在四份02.1—02.4业务规则接入、文档导航同步后重新核验：

- 公共加工六专题＋118141联合检查：**208项通过，失败0、跳过0**。其中118141覆盖单CASE接线、每个片段变更的可检测性、全部原始token与92列顺序、四条收入路径和真实规则链接。
- 本轮首次完整目录检查的快照为375条本地链接、失效0、空文件0。随后其他并行任务新增对内／香港收入和佣金导航，独立复核曾发现这些新导航的目标尚未落地，因此**该快照不代表持续变化的整个公共目录当前零断链**，也不将并行任务新增内容纳入本次交付完成声明。
- 2026-09-22 16:11（北京时间）再次限定检查本轮118141目录：**74条本地链接，失效0**，包含四份规则SQL、三份业务说明和直接上游入口；本目录链接及guide→module检查也已纳入自动化测试。未覆盖或改写其他任务新增的114013、220979、佣金专题。
- 独立复核确认：四个片段实际参与主查询，13个WHEN及无ELSE保持原样；原有参数表达式对照未被削弱；三个新业务说明的数值、日期和分摊范围与SQL一致。
- 所有生产来源快照和原始业务规则保留；这些结果证明本次整理的对应关系及已列举构造场景，不证明生产执行、实际数据完整性或待确认制度依据。
