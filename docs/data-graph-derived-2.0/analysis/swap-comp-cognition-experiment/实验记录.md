# 实验记录

日期：2026-09-09。性质：一次性有界业务认知实验，未改任何代码、Facts、图或既有产物；本目录两份文档为唯一新增。

## 实际读取的材料与用途

| 材料                                                                                                                                         | 用途                                                   | 属补读？                                                             |
| -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ | -------------------------------------------------------------------- |
| `swap-comp-pilot/README.md`                                                                                                                  | 定位覆盖账本与证据路径                                 | 否（指定入口）                                                       |
| `swap-comp-pilot/coverage.csv`（仅筛 218601/204172 两行）                                                                                    | 拿到两任务的证据文件路径与哈希                         | 否（指定入口）                                                       |
| 218601 `evidence-v3.json`（2.87MB，未通读：用 node 按需抽取 relations 的 join condition_display、datasetIo、bindings[0]、statement raw_sql） | 过程 A 全部证据                                        | 否（指定范围）                                                       |
| 204172 `evidence-v3.json`（162KB，全文可用，同样按需抽取）                                                                                   | 过程 B 全部证据                                        | 否（指定范围）                                                       |
| `swap-comp-pilot/互换合约表业务说明.md`（51 行）                                                                                             | 核对键的形成侧角色、复用表位置描述与"键不等于主键"结论 | 是（补读 1/2）：需要回答过程 A 问题 3 的键对齐对象，避免凭字段名臆测 |
| 两任务的 raw SQL（从 Facts statement 提取到临时文件后通读：218601 共 683 行、204172 共 72 行）                                               | 加工顺序、表达式、窗口与分支的直接依据                 | 否（属于指定任务核读）                                               |

## 复用 vs 新增认识

复用自试点（已在文中标注或属背景）：表的形成方式与分区、键不能当主键、204172 的对手方简称映射 busi_type、汇率回退结构的存在。
本次新增认识：

- 218601 的完整对象关系与加工顺序：持仓为主驱动、合约快照为 INNER JOIN 对端、终止锚点三级回退（Valu_Trmt_Date → Actl_Sett_Date → End_Pric_Date）、存续/到期两分支的 `<` 与 `=` 分界、ytd 用上年年末持仓做差、腿损益 CTE 的三列分组键。
- `trscr`（T05_OTC_COMP_RGST_SAC_EVT）是全任务唯一无日期条件、无聚合的合约级 LEFT JOIN → 具名扩行通道；而 TLEG/H/PTLV 因 GROUP BY 键=JOIN 键在结构上至多一行。这把试点泛泛的"LEFT JOIN 可能扩行"落到了具体对象。
- 204172 三指标的"存量×动态本金 vs 新增×初始本金"字段错配 + 窗口语义差异；币种 CASE 无 ELSE 时敞口静默消失（nvl 不可区分）。
- Facts 的 `condition_display` 逐条与原 SQL 比对一致，验证了试点警告的"JOIN 对端混入"在当前图版本的两个任务上不存在。

## 补读次数与原因

补读 1 次（互换合约表业务说明.md）。第 2 份额度未使用：其余问题均可由两任务原始 Facts/SQL 回答，无明确用途的额外材料不读。

## 最关键的未解问题

1. `trscr` 事件表按 `Swap_Comp_Agt_Id` 连接是否一对多（决定 218601 输出是否实际扩行）——静态 SQL 只能证明通道存在。
2. 合约快照表 `(busi_date, Swap_Comp_Agt_Id)` 与持仓表 `(busi_date, Src_Prd_Id)` 的键唯一性。
3. 状态码 101/226/218、账簿 10218/10231 的业务定义。

## 成本

- Token 消耗：不可获得（本环境不暴露本次会话 token 计量）。
- 调用量：约 20 次工具调用（文件/目录列举 6、CSV/Facts 解析与抽取 9、文档读写 5）；未运行任何项目命令、未重跑全图、未新增脚本、未用子 agent。
- 耗时：不可获得精确计时；交互轮次约 15 轮。
- 输出体量控制：Facts 全文 2.87MB 仅抽取片段查看；两次临时 SQL 落盘于 `C:\Users\13246\.qoder-cn\`（会话级临时文件，非仓库内容）。
