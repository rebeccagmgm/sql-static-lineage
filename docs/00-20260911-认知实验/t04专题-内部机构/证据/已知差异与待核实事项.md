# 已知差异与待核实事项

[返回入口](00-20260911-认知实验/t04专题-内部机构/README.md)

这些记录用于限制正文结论，不表示当前线上已发生错误，也不是本次代码修复任务。发现于 2026-09-14。

| 事项 | 已确认事实 | 影响与仍需核实的证据 |
|---|---|---|
| 机构主表被标成拉链 | 平台描述写历史拉链；字段没有开始结束区间，PAS 分支覆盖来源、OA 分支维护存量 | 按来源解释时间；不能把主表统一作为 SCD 区间表 |
| 旧来源标签与真实来源分离 | PAS 机构／员工／任职保留 ERP 或 MMS 标签，IOA 保留 OAS 标签 | 源身份判断应同时看 real_src_tbl 和实际读取；标签不是物理生产者证明 |
| 同任务两份 SQL 不等长 | 41 个样例与对应文件缓存比较，8 个正文不一致；61609 文件缓存多出完整 CTE | 两份并存，明确来源和业务日期；不能把较晚采集解释成代码新版本或较新业务事实 |
| PAS 机构关系的前置 CTE | SQLite 61609 缺定义，文件缓存已补回 | 已补全日期转换、期间合并及分公司归属解释；不再把该 CTE 当完全缺失 |
| 前置员工名单缺构造 | 190085 只显示 TEMP1/2/3 合并，文件缓存亦如此 | 尚未还原三份名单如何完整形成；不能声称全人员范围已审计 |
| 行政第七级判断冲突 | 145723 两份证据都同时要求 lvl6 非空和为空 | 该分支逻辑不可满足；需核执行版本和实际层级数据确认影响 |
| 机构主表删除 CASE 不可达 | 61476 源限定状态 1/2，投影另有状态 9 删除分支 | 不以该 CASE 证明来源删除机构都入仓；需确认预期删除口径 |
| 角色权限结构差异 | 246059 DDL／投影含 Righ_Id、User_Righ_Cd；物理快照为 Righ_Cd、无 Righ_Id | 不拼接成一份“确定最新结构”；应补实时 DDL、字段顺序和写入版本 |
| 招聘面试字段冲突 | 97715 为 Intv_Rnk_Cd／面试轮次；元数据为 Intv_Stat_Cd／面试状态 | 字典及含义不能混用；需核实际表结构与源枚举 |
| 学分排序未实际过滤 | 88170 计算 RN，但没有 RN=1；LEFT JOIN 后过滤 B.LOGIN_ID 非空 | 无法据注释证明去重，存在范围收窄；实际扩行需数据确认 |
| 财务段关系码缺本地定义 | PAS SQL 写关系 21/22；CD018 本地只有另外 10 个值 | 保留 SQL 注释含义，不能冒充已命中字典或擅自补规范码值 |
| 考核结果时间条件 | 182044 只有源日期下界，没有上界，保留源日期动态分区 | 不称严格过去十二个月；不把月度／年度得分跨日期直接相加 |
| 分成单位与守恒未知 | WMP 按成员角色复制团队比例，DIA 直接取源 TCBL | 未证明 0–1 或百分数，也未证明成员合计规则；需源定义与行数据 |
| TIT 配置连接基数未知 | 配置保留源 ID，下游只按部门连接 | 无法证明一部门一条配置；重算历史时使用何时配置也待业务确认 |
| 营业部地址连接身份 | 61593 地址连接使用 B.INR_ORG_ID，而映射另一端才是柜台码 | 原样记录实际表达式；需来源键和数据匹配验证，未自行“修正”SQL |
| 同名任务并存 | 165638 与 211773 名称相同、配置投影相近 | 未确认启停、部署范围和唯一在用生产者；不选一个冒充唯一事实 |
| 元数据新旧覆盖不同 | 新平台目录多出 13 个 pdata_n 对象，旧字段目录未匹配 | 已入补充目录；不推断其他缺失对象已下线 |
| 图库不可用 | status 返回 ARCADEDB_UNAVAILABLE | 仅交付 SQL 复核的解释路径，不声明图谱闭包或全消费者清单 |

## 对整域覆盖的限制

六个主要分支均有对象、字段与真实加工案例。精读集中在通用机构、人事、OA／若干系统授权、PAS 团队、WMP 分成、AUM 考核、LMS 培训、PAS 招聘和 ECR 商机。香港名片、部分投行／受限模型、自营交易员权限、全部系统用户来源、所有考核算法和所有商机类别仍未逐源审计。

这不妨碍使用专题建立整体认识，但不能把“逐表目录覆盖”解释成“全源、全表、全字段、运行、唯一性和业务正确性全部通过”。后续只在具体业务问题需要时补对应证据。
