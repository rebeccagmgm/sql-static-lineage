# 本轮人工复核 SQL 摘录

来源：只读 schedule-evidence.sqlite 的 evidence 表，evidence_type=hive-task。每段使用原 payload 行号；SQL 哈希用于定位本地版本。仅列出建模有关片段，未提供完整可执行脚本。AVAILABLE 表示留有 SQL，固定日期与观测日期不等同，不能据此证明生产已运行。

## 任务 128885：GPB 主体编号与去重

- 证据哈希：603a5d9f4979bffc5ea9ca2efa74def6418f08a7b381eb2dfd15dbfe920d7e29
- 观测时间：2026-09-05T01:06:51.399Z

原文第 34–49 行：

```sql
SELECT DISTINCT 
     CONCAT('GPB010-',BROKERID)               AS  Pty_Id                  --当事人编号    
    ,BROKERNAME                               AS  Pty_Name                --当事人名称    
    ,'280202'                                 AS  Pty_Cate_Cd             --当事人类别代码
    ,''                                       AS  Pty_Stat_Cd             --当事人状态代码
    ,''                                       AS  Oact_Date               --开户日期      
    ,''                                       AS  Clos_Date               --销户日期      
    ,'${data_src_cd}'                    AS  Data_Src_Cd             --数据来源代码
    ,'${src_table}'                      AS  Src_Tbl                 --源表
    ,'${filename}'                       AS  Task_Name               --任务名
    ,'${data_day_str}'                   AS  Data_Etl_Date           --数据加载日期
    ,'${data_day_str}'                   AS  Data_Upt_Date           --数据更新日期
    ,'${src_table}'                      AS  Real_Src_Tbl            --真实源表
FROM  (SELECT *,ROW_NUMBER()OVER(PARTITION BY BROKERID ) RN
     FROM ${src_table}  WHERE BUSI_DATE='${data_day_str}' )A
WHERE RN = 1
```

原文第 70–84 行：

```sql
        ,CASE WHEN A.Pty_Id IS NULL     AND B.Pty_Id IS NOT NULL THEN 'I' --新增
              WHEN A.Pty_Id IS NOT NULL AND B.Pty_Id IS NULL THEN 'D' --删除
              WHEN A.Pty_Id IS NOT NULL AND B.Pty_Id IS NOT NULL AND (
                     COALESCE(A.Pty_Name                ,'') <> COALESCE(B.Pty_Name                ,'')
                  OR COALESCE(A.Pty_Cate_Cd             ,'') <> COALESCE(B.Pty_Cate_Cd             ,'')
                  OR COALESCE(A.Pty_Stat_Cd             ,'') <> COALESCE(B.Pty_Stat_Cd             ,'')
                  OR COALESCE(A.Oact_Date               ,'') <> COALESCE(B.Oact_Date               ,'')
                  OR COALESCE(A.Clos_Date               ,'') <> COALESCE(B.Clos_Date               ,'')
             ) THEN 'U' --变更
             ELSE 'S' --无变更
             END                 AS DATA_TYPE              --数据类型
FROM  (SELECT * FROM T01_PTY 
                WHERE SRC_TBL='ODATA_N_GPB.U_BROKERLIST')A
FULL OUTER JOIN ${DB_TEMP}.T01_PTY_TEMP_GPB026 B
ON    A.Pty_Id=B.Pty_Id
```

## 任务 128887：GPB 名称快照

- 证据哈希：8d9a2b7a004a4c6e0fee084bbe70bc0f17bdaca65b4f5e97829d1bf77d63da9d
- 观测时间：2026-09-05T01:06:51.404Z

原文第 26–44 行：

```sql
INSERT OVERWRITE TABLE T01_PTY_NAME PARTITION(SRC_TBL='${src_table}',BUSI_DATE='${data_day_str}')
-----------------------------------------------------------------------------------------------------
--Group47: Source Table:[ODATA_N_GPB.U_BROKERLIST:券商清单] 
-----------------------------------------------------------------------------------------------------
SELECT DISTINCT 
     CONCAT('GPB010-',BROKERID)                        AS  Pty_Id                  --当事人编号
    ,'280202'                                          AS  Pty_Cate_Cd             --当事人类别代码 --280202 投易通PB经纪商
    ,BROKERNAME                                        AS  Full_Name_Ch            --中文全称
    ,BROKERFULLNAME                                    AS  Shor_Name_Ch            --中文简称
    ,''                                                AS  Full_Name_En            --英文全称
    ,''                                                AS  Shor_Name_En            --英文简称
    ,'${data_src_cd}'                             AS  Data_Src_Cd             --数据来源代码
    ,'${filename}'                                AS  Task_Name               --任务名
    ,'${data_day_str}'                            AS  Data_Etl_Date           --数据加载日期
    ,'${data_today_str}'                          AS  Data_Upt_Date           --数据更新日期
    ,'${data_today}'                              AS  Data_Time               --数据时间
FROM  (SELECT *,ROW_NUMBER()OVER(PARTITION BY BROKERID ) RN
     FROM ODATA_N_GPB.U_BROKERLIST WHERE BUSI_DATE='${data_day_str}' )A
WHERE RN = 1
```

## 任务 91801：CIP 多账户展开及关系匹配键

- 证据哈希：c3e61c689a894445afb2f5b55149ef17f5b316319f3284d7314ac276660279e3
- 观测时间：2026-09-05T01:06:39.088Z

原文第 42–65 行：

```sql
SELECT
        CONCAT('TA5001-',A.C_FUNDACCO)                     AS Pty_Id1                  --当事人编号
       ,'01'                                               AS Pty_Rela_Type_Cd1        --当事人关系类型代码  同一个当事人
       ,CASE 
         WHEN C_CUSTTYPE = '个人' THEN '111700' 
         WHEN C_CUSTTYPE in ('机构','产品') THEN '211700' 
         ELSE '' 
        END                                                AS Pty_Cate_Cd1             --当事人类别代码
       ,CONCAT('CIP001-',A.C_CUSTNO)                       AS Rela_Pty_Id1             --关联当事人编号
       ,CASE 
         WHEN C_CUSTTYPE = '个人' THEN '111700' 
         WHEN C_CUSTTYPE in ('机构','产品') THEN '211700' 
         ELSE '' 
        END                                                AS Rela_Pty_Cate_Cd1        --关联当事人类别代码
       ,'CIP'                                              AS Data_Src_Cd1             --数据来源代码
       ,'ODATA_N_CIP.T_V_CUSTOMERINFO'                     AS Src_Tbl1                 --源表
       ,UPPER('${filename}')                          AS Task_Name1               --任务名
  FROM (SELECT t.C_CUSTNO,t.C_CUSTTYPE,t.C_FUNDACCO
        FROM(SELECT C_CUSTNO,C_CUSTTYPE,C_FUNDACCO
             FROM ODATA_N_CIP.T_V_CUSTOMERINFO WHERE BUSI_DATE = '${data_day_str}' and c_custname not like '%测试%')t
        LATERAL VIEW EXPLODE(SPLIT(C_FUNDACCO,'\\\\\\|'))t AS C_FUNDACCO
        GROUP BY t.C_CUSTNO,t.C_CUSTTYPE,t.C_FUNDACCO
       )A
;
```

原文第 81–90 行：

```sql
FROM   (  SELECT
            *
          FROM  T01_PTY_RELA_H
          WHERE STRT_DATE <='${data_day_str}'
            AND END_DATE  > '${data_day_str}'
            AND SRC_TBL ='ODATA_N_CIP.T_V_CUSTOMERINFO'  )A   --历史(昨日)开链数据
FULL OUTER JOIN TEMP.T01_PTY_RELA_H_TEMP_CIP001 B               --当天的数据
ON        A.PTY_ID            = B.PTY_ID1
AND       A.PTY_RELA_TYPE_CD  = B.PTY_RELA_TYPE_CD1
AND       A.Rela_Pty_Id       = B.Rela_Pty_Id1
```

## 任务 76047：CMI 关系全量覆盖与固定区间

- 证据哈希：718f92e458552d05748439142c40947b3b18578fae4a86667a4094668535660b
- 观测时间：2026-09-05T01:06:33.358Z

原文第 40–67 行：

```sql
INSERT OVERWRITE TABLE T01_PTY_RELA_H PARTITION(SRC_TBL)
SELECT
        A.PTY_ID                                           AS PTY_ID                  --当事人编号
       ,'01'                                               AS PTY_RELA_TYPE_CD        --当事人关系类型代码
       ,'211001'                                           AS PTY_CATE_CD             --当事人类别代码
       ,'1900-01-01'                                       AS STRT_DATE               --开始日期
       ,B.PTY_ID                                           AS RELA_PTY_ID             --关联当事人编号
       ,'190000'                                           AS RELA_PTY_CATE_CD        --关联当事人类别代码
       ,'2099-12-31'                                       AS END_DATE                --结束日期
       ,'CMI'                                              AS DATA_SRC_CD             --数据来源代码
       ,UPPER('${filename}')                          AS TASK_NAME               --任务名
       ,'${data_today}'                               AS DATA_TIME 
       ,'ODATA_N_CMI.G_TB_CST_UNIT'                        AS SRC_TBL                 --源表
  FROM (
    SELECT 
        CONCAT('MOT004-',C_TENANTID)   AS PTY_ID,
        C_MANAGERCREDITCODE            AS MNGR_CD
    FROM ODATA_N_CMI.A_HSI_TENANT --管理人信息表   
    WHERE BUSI_DATE = '${data_day_str}'
  ) A
  JOIN (
    SELECT 
        CONCAT('CMI003-',OBJID)        AS PTY_ID,
        LICENSE                        AS MNGR_CD
        FROM ODATA_N_CMI.G_TB_CST_UNIT
        WHERE BUSI_DATE = '${data_day_str}'
          AND REPORT_TYPE = 'all'
  ) B ON A.MNGR_CD = B.MNGR_CD;
```

## 任务 179723：同一分区五类评级与真实来源差异

- 证据哈希：11ebb4a362c67d4c4c1d6e3711b5504a705852d4337dfc105214a26d68267227
- 观测时间：2026-09-05T01:07:12.915Z

原文第 12–18 行：

```sql
INSERT OVERWRITE TABLE T01_PTY_RAT PARTITION(Busi_Date = '${data_day_str}', Src_Tbl = 'ODATA_N_OOM.F_OTC_DERIVATIVE_COUNTERPARTY')
SELECT
            A.CLIENT_ID                             AS  Pty_Id                      --当事人编号
           ,'PI_LVL'                                AS  Rat_Type_Cd                 --评级类型代码
           ,'210501'                                AS  Pty_Cate_Cd                 --当事人类别代码
           ,''                                      AS  Rat_Date                    --评级日期
           ,NVL(B.DW_CD_VAL,A.PROFESSIONAL_INVESTORS)   AS  Rat_Cd                      --评级结果
```

原文第 43–49 行：

```sql
INSERT INTO TABLE T01_PTY_RAT PARTITION(Busi_Date = '${data_day_str}', Src_Tbl = 'ODATA_N_OOM.F_OTC_DERIVATIVE_COUNTERPARTY')
SELECT
            A.CLIENT_ID                             AS  Pty_Id                      --当事人编号
           ,'RISK_LVL'                              AS  Rat_Type_Cd                 --评级类型代码
           ,'210501'                                AS  Pty_Cate_Cd                 --当事人类别代码
           ,''                                      AS  Rat_Date                    --评级日期
           ,A.RISK_LEVEL                            AS  Rat_Cd                      --评级结果
```

原文第 67–84 行：

```sql
INSERT INTO TABLE T01_PTY_RAT PARTITION(Busi_Date = '${data_day_str}', Src_Tbl = 'ODATA_N_OOM.F_OTC_DERIVATIVE_COUNTERPARTY')
SELECT
            A.CLIENT_ID                             AS  Pty_Id                      --当事人编号
           ,'AML_LVL'                               AS  Rat_Type_Cd                 --评级类型代码
           ,'210501'                                AS  Pty_Cate_Cd                 --当事人类别代码
           ,INIT_DATE                               AS  Rat_Date                    --评级日期
           ,A.RISK_TYPE                             AS  Rat_Cd                      --评级结果
           ,''                                      AS  Rat_Org                     --评级机构
           ,''                                      AS  Rat_Eff_Date                --评级生效日期
           ,''                                      AS  Rat_Exp_Date                --评级到期日期
           ,'0'                                     AS  Dyna_Rat_Type_Cd            --动态测评类型代码
           ,'OIS'                                   AS  Data_Src_Cd                 --数据来源代码
           ,UPPER('${filename}')               AS  Task_Name                   --任务名
           ,'${data_day_str}'                  AS  Data_Etl_Date               --数据加载日期
           ,'${data_day_str}'                  AS  Data_Upt_Date               --数据更新日期
           ,'${data_today}'                    AS  Data_Time                   --数据时间
           ,'ODATA_N_OOM.F_AML_DERIV_RISKREP'       AS  Real_Src_Tbl                --真实源表
FROM       (SELECT * FROM ODATA_N_OOM.F_AML_DERIV_RISKREP WHERE Busi_Date = '${data_day_str}' )A
```

原文第 91–98 行：

```sql
INSERT INTO TABLE T01_PTY_RAT PARTITION(Busi_Date = '${data_day_str}', Src_Tbl = 'ODATA_N_OOM.F_OTC_DERIVATIVE_COUNTERPARTY')
SELECT
            A.CLIENT_ID                             AS  Pty_Id                      --当事人编号
           ,'CRED_LVL'                              AS  Rat_Type_Cd                 --评级类型代码
           ,'210501'                                AS  Pty_Cate_Cd                 --当事人类别代码
           ,''                                      AS  Rat_Date                    --评级日期
           ,IF(NVL(TRIM(A.CREDIT_RATING),'')='无','',A.CREDIT_RATING)
                                                    AS  Rat_Cd                      --评级结果
```

原文第 115–122 行：

```sql
INSERT INTO TABLE T01_PTY_RAT PARTITION(Busi_Date = '${data_day_str}', Src_Tbl = 'ODATA_N_OOM.F_OTC_DERIVATIVE_COUNTERPARTY')
SELECT
            A.CLIENT_ID                             AS  Pty_Id                      --当事人编号
           ,'OTC_CUTP_LVL'                              AS  Rat_Type_Cd                 --评级类型代码
           ,'210501'                                AS  Pty_Cate_Cd                 --当事人类别代码
           ,''                                      AS  Rat_Date                    --评级日期
           ,NVL(B.DW_CD_VAL,A.CLIENT_TYPE) 
                                                    AS  Rat_Cd                      --评级结果
```

## 任务 104300：TIT 扩展参数评级

- 证据哈希：8ef23022ef45c9486c4a1b9870f69a677bb9dabef0f3335875b7fa228081b7a6
- 观测时间：2026-09-05T01:06:44.298Z

原文第 30–53 行：

```sql
INSERT OVERWRITE TABLE T01_PTY_RAT PARTITION(BUSI_DATE = '${data_day_str}', SRC_TBL = '${src_table}')
-----------------------------------------------------------------------------------------------------
--Group52: Source Table:[ODATA_N_TIT.D_REF_CTPTY_EXT_PARAM:交易对手属性参数表 --场外交易对手等级
-----------------------------------------------------------------------------------------------------
SELECT  
       CONCAT('TIT060-',A.KEY_CTPTY_ID)                   AS PTY_ID                    --当事人编号
      ,'OTC_CUTP_LVL'                           AS RAT_TYPE_CD               --评级类型代码
      ,IF(B.CTPTY_TYPE='INDIVIDUAL' ,'190400','290203') 
                                                AS PTY_CATE_CD               --当事人类别代码
      ,''                                       AS RAT_DATE                  --评级日期
      ,NVL(A.DEALER_LEVEL,'')                   AS RAT_CD                    --评级结果
      ,''                                       AS RAT_ORG                   --评级机构
      ,''                                       AS RAT_EFF_DATE              --评级生效日期
      ,''                                       AS RAT_EXP_DATE              --评级到期日期
      ,''                                       AS DYNA_RAT_TYPE_CD          --动态测评类型代码
      ,'${data_src_cd}'                    AS DATA_SRC_CD               --数据来源代码
      ,UPPER('${filename}')                AS TASK_NAME                 --任务名
      ,'${data_day_str}'                   AS DATA_ETL_DATE             --数据加载日期
      ,'${data_day_str}'                   AS DATA_UPT_DATE             --数据更新日期
      ,'${data_today}'                     AS DATA_TIME                 --数据时间
      ,'ODATA_N_TIT.D_REF_CTPTY_EXT_PARAM'         AS  Real_Src_Tbl             --真实源表
  FROM (SELECT * FROM ODATA_N_TIT.D_REF_CTPTY_EXT_PARAM WHERE BUSI_DATE = '${data_day_str}')A
  LEFT JOIN(SELECT *  FROM ODATA_N_TIT.D_REF_COUNTERPARTY WHERE  BUSI_DATE='${data_day_str}' )B
         ON A.KEY_CTPTY_ID=B.ID
```

## 任务 105077：TIT 主体源评级

- 证据哈希：e551d3baf644c666eeb6403e9268efcdd592f6a48d84834654596c9e90f4ad45
- 观测时间：2026-09-05T01:06:44.625Z

原文第 30–51 行：

```sql
INSERT OVERWRITE TABLE T01_PTY_RAT PARTITION(BUSI_DATE = '${data_day_str}', SRC_TBL = '${src_table}')
-----------------------------------------------------------------------------------------------------
--Group34: Source Table:[ODATA_N_TIT.D_REF_COUNTER_PARTY:交易对手] --场外交易对手等级
-----------------------------------------------------------------------------------------------------
SELECT  
       CONCAT('TIT060-',A.ID)                   AS PTY_ID                    --当事人编号
      ,'OTC_CUTP_LVL'                           AS RAT_TYPE_CD               --评级类型代码
      ,IF( CTPTY_TYPE='INDIVIDUAL' ,'190400','290203') 
                                                AS PTY_CATE_CD               --当事人类别代码
      ,''                                       AS RAT_DATE                  --评级日期
      ,NVL(A.DEALER_LEVEL,'')                   AS RAT_CD                    --评级结果
      ,''                                       AS RAT_ORG                   --评级机构
      ,''                                       AS RAT_EFF_DATE              --评级生效日期
      ,''                                       AS RAT_EXP_DATE              --评级到期日期
      ,''                                       AS DYNA_RAT_TYPE_CD          --动态测评类型代码
      ,'${data_src_cd}'                    AS DATA_SRC_CD               --数据来源代码
      ,UPPER('${filename}')                AS TASK_NAME                 --任务名
      ,'${data_day_str}'                   AS DATA_ETL_DATE             --数据加载日期
      ,'${data_day_str}'                   AS DATA_UPT_DATE             --数据更新日期
      ,'${data_today}'                     AS DATA_TIME                 --数据时间
      ,'${src_table}'                      AS  Real_Src_Tbl             --真实源表
  FROM (SELECT * FROM ${src_table} WHERE BUSI_DATE = '${data_day_str}')A
```

## 任务 63792：名称 DDL 与 SELECT 不一致

- 证据哈希：9225028df9e20b185926a86091592e9ec3923442c3d132ca33776a41009f45f6
- 观测时间：2026-09-05T01:06:23.155Z

原文第 9–24 行：

```sql
CREATE TABLE IF NOT EXISTS T01_PTY_NAME(
     Pty_Id         string comment'当事人编号',
     Strt_Date      string comment'开始日期',
     Pty_Cate_Cd    string comment'当事人类别代码',
     Full_Name_Ch   string comment'中文全称',
     Shor_Name_Ch   string comment'中文简称',
     Full_Name_En   string comment'英文全称',
     Shor_Name_En   string comment'英文简称',
     End_Date       string comment'结束日期',
     Data_Src_Cd    string comment'数据来源代码',
     Task_Name      string comment'任务名',
     Data_Etl_Date  string comment'数据加载日期',
     Data_Upt_Date  string comment'数据更新日期',
     DATA_TIME      string comment'数据时间'
)COMMENT '当事人名称'
PARTITIONED BY (Src_Tbl string comment'源表',BUSI_DATE  STRING COMMENT '业务日期')
```

原文第 28–50 行：

```sql
INSERT OVERWRITE TABLE T01_PTY_NAME PARTITION(Src_Tbl,BUSI_DATE)
-----------------------------------------------------------------------------------------------------
--Group11: Source Table:[ODATA_N_TA5.D_TA_TCUSTOMERINFO:账户资料表]
-----------------------------------------------------------------------------------------------------

SELECT
        Concat('TA5001-',A.C_FUNDACCO)     as Pty_Id               --当事人编号
       ,CASE WHEN B.C_CUSTTYPE='1' THEN'110300'
             ELSE '210300' END             as Pty_Cate_Cd          --当事人类别代码
       ,B.C_CUSTNAME                       as Full_Name_Ch         --中文全称
       ,IF(B.C_SHORTNAME IS NULL OR TRIM(B.C_SHORTNAME)='',B.C_CUSTNAME,B.C_SHORTNAME)   as Shor_Name_Ch         --中文简称
       ,''                                 as Full_Name_En         --英文全称
       ,''                                 as Shor_Name_En         --英文简称
       ,'TA5'                              as DATA_SRC_CD          --数据来源代码
       ,UPPER('${filename}')          as TASK_NAME            --任务名
       ,'${data_day_str}'             as DATA_ETL_DATE        --数据加载日期
       ,'${data_day_str}'             as DATA_UPT_DATE        --数据更新日期
       ,'${data_today}'               as DATA_TIME            --数据时间
       ,'ODATA_N_TA5.D_TA_TCUSTOMERINFO'   as SRC_TBL              --源表
       ,'${data_day_str}'             as BUSI_DATE            --业务日期
FROM  (SELECT * FROM  ${src_table_ta5}.D_TA_TCUSTOMERINFO WHERE BUSI_DATE='${data_day_str}')A
LEFT JOIN(SELECT *  FROM ${src_table_ta5}.D_TA_TACCOINFO WHERE BUSI_DATE='${data_day_str}')B
       ON A.C_FUNDACCO=B.C_FUNDACCO
```

## 任务 231126：死亡材料 SQL 与元数据冲突

- 证据哈希：89014ff779bdaa06f4f12af7bbd035ed9a125aff94bc9b192592528acd96527c
- 观测时间：2026-09-05T07:23:03.908Z

原文第 9–24 行：

```sql
CREATE TABLE IF NOT EXISTS T01_PTY_DECES_MATRL_INFO (
  Pty_Id STRING COMMENT '当事人编号',
  Pty_Cate_Cd STRING COMMENT '当事人类别代码',
  Acode_Acct STRING COMMENT '一码通账户',
  Deces_Matrl_Id STRING COMMENT '死亡材料编号',
  Deces_Matrl_Name STRING COMMENT '死亡材料名称',
  Matrl_Archv_Date STRING COMMENT '材料归档日期',
  Data_Src_Cd STRING COMMENT '数据来源代码',
  Task_Name STRING COMMENT '任务名',
  Data_Etl_Date STRING COMMENT '数据加载日期',
  Data_Upt_Date STRING COMMENT '数据更新日期',
  Data_Time STRING COMMENT '数据时间',
  Real_Src_Tbl STRING COMMENT '真实源表'
)
COMMENT '当事人死亡材料信息'
PARTITIONED BY (SRC_TBL STRING COMMENT'源表')
```

原文第 28–48 行：

```sql
INSERT OVERWRITE TABLE T01_PTY_DECES_MATRL_INFO PARTITION(SRC_TBL)
SELECT
cust_pty_no                           AS  Pty_Id                  --当事人编号
,'110100'                              AS  Pty_Cate_Cd             --当事人类别代码
,acode_code                            AS  Acode_Acct              --一码通账户
,IF(NVL(TRIM(ID),'')='','',concat('EAI005-',ID))
AS  Deces_Matrl_Id          --死亡材料编号
,suff_name                             AS  Deces_Matrl_Name        --死亡材料名称
,src_busi_date                         AS  Matrl_Archv_Date        --材料归档日期
,'EAI'                 AS  Data_Src_Cd             --数据来源代码
,'PDATA_N.T01_PTY_DECES_MATRL_INFO_EAI005'                    AS  Task_Name               --任务名
,'2026-05-24'                AS  Data_Etl_Date           --数据加载日期
,'2026-05-24'                AS  Data_Upt_Date           --数据更新日期
,'2026-05-25 00:36:58'                  AS  Data_Time               --数据时间
,'ODATA_N_EAI.E_T_ARCV_CUST_PASSAWAY'                   AS  Real_Src_Tbl            --真实源表
,'ODATA_N_EAI.E_T_ARCV_CUST_PASSAWAY'                   AS  Src_Tbl                 --源表
FROM   (SELECT *  FROM ODATA_N_EAI.E_T_ARCV_CUST_PASSAWAY WHERE  BUSI_DATE='2026-05-24' )A
;

```

## 任务 63338：账户消费主体与机构客户的不同筛选

- 证据哈希：e80f034990219bdb69d96e559412256a2a6475c61ad5c93b2c308eb5fb96b331
- 观测时间：2026-09-01T13:30:38.338Z

原文第 73–76 行：

```sql
left join (select *
from pdata_n.t01_pty
where src_tbl = 'ODATA_N_RCC.A_CLIENT') cust
on acct.client_id = cust.pty_id
```

原文第 99–105 行：

```sql
join (select *
from pdata_n.t01_corp_cust
where busi_date=default.pretradedate(date_add('2026-05-18',1),1)
and del_flag='0'
and src_tbl='ODATA_N_RCC.A_ORGANINFO'
and corp_cust_clas_cd in ('5','6') ) cust
on acct.agt_holder = cust.pty_id
```

## 任务 71118：消费者拆解来源主体 ID

- 证据哈希：380db598eecb6fb1ae502305f5ae00505a856eeb7a54c53abdc27150d0664c5c
- 观测时间：2026-09-03T06:48:55.750Z

原文第 49–56 行：

```sql
left join (select split(pty_id,'-')[1] ta_acct,
pty_name
from pdata_n.T01_PTY
where SRC_TBL ='ODATA_N_TA5.D_TA_TCUSTOMERINFO') ta_cust
on ta_trd_acct.ta_acct = ta_cust.ta_acct
group by ta_trd_acct.ta_acct,
ta_trd_acct.agency_no,
ta_trd_acct.trd_acct) t1
```

原文第 65–71 行：

```sql
select t1.*
from (select map('fund_account',split(pty_id,'-')[1]) real_account,
max(pty_name) account_name,
'TA.FND_ACCT' account_type_code
from pdata_n.T01_PTY
where SRC_TBL ='ODATA_N_TA5.D_TA_TCUSTOMERINFO'
group by split(pty_id,'-')[1]) t1
```

原文第 79–85 行：

```sql
from (select map('client_id',pty_id)          real_account,
pty_name                      account_name,
'RMS.RD_CLIENT'               account_type_code
from pdata_n.t01_pty
where pty_cate_cd = '210600'
and src_tbl = 'ODATA_N_RMS.N_CRM_CUSTOMER'
group by pty_id,pty_name
```

## 任务 122891：研究销售收入名称匹配和择一

- 证据哈希：7cd2388426a43621cd9d8aef73e4e31990273952e74f7b77c18d1caff337ba5d
- 观测时间：2026-09-03T07:26:54.168Z

原文第 8–15 行：

```sql
-- createSql
CREATE TABLE if not exists tmp_seat_info AS WITH temp_rd_sale_income_dtl_pty_name AS (
  select distinct pn.pty_id,
  pn.full_name_ch,
  pn.shor_name_ch,
  ps.stati_cont_desc as alias_name from (select * from pdata_n.t01_pty_name where src_tbl = 'ODATA_N_RMS.N_CRM_CUSTOMER' and busi_date='2026-08-06' ) pn left join (select pty_id,stati_cont_desc from PDATA_N.T01_PTY_STATI_INFO_H where src_tbl='ODATA_N_RMS.N_CRM_CUSTOMER_ALIAS' and strt_date<='2026-08-06' and end_date > '2026-08-06' ) ps on ps.pty_Id =pn.pty_id
)
select * from ( select t1.seat_no ,t1.src_busi_date ,cast(t1.agt_income_rate AS decimal(10,4)) AS agt_income_rate ,cast(t1.hk_income_rate AS decimal(10,4)) AS hk_income_rate ,t1.year AS year ,t1.month AS month ,cast(floor(cast(t1.month as int) /3.1)+1 AS int) AS quarter ,substr(regexp_replace(t1.src_busi_date ,'-',''),1,6) AS busi_date ,t1.js_typevalue as typevalue ,coalesce(t2.cust_id,t6.pty_id,t7.pty_id,t8.pty_id) as cust_id ,t9.pty_name as cust_name ,row_number() over(partition by t1.seat_no,coalesce(t2.cust_id,t6.pty_id,t7.pty_id,t8.pty_id) ,t1.year,t1.month order by src_busi_date desc ) as rn from (select * from pdata_nds.n_v_cockpit_seat_base_info where busi_date='2026-08-06')t1 LEFT JOIN PDATA_N.T01_RMS_CUST_SEAT_INFO t2 ON t1.Seat_No = t2.Seat_No AND t2.mth = concat(t1.year,lpad(t1.month,2,'0')) AND t2.src_tbl = 'ODATA_N_RMS.P_COCKPIT_CUSTOMER_SEAT_TYPE' LEFT JOIN temp_rd_sale_income_dtl_pty_name t6 ON t1.unit_name = t6.full_name_ch LEFT JOIN temp_rd_sale_income_dtl_pty_name t7 ON t1.unit_name = t7.shor_name_ch LEFT JOIN temp_rd_sale_income_dtl_pty_name t8 ON t1.unit_name=t8.alias_name LEFT JOIN (select * from pdata_n.t01_pty where src_tbl = 'ODATA_N_RMS.N_CRM_CUSTOMER') t9 ON coalesce(t2.cust_id,t6.pty_id,t7.pty_id,t8.pty_id)=t9.pty_id )aa where rn=1 ;
```

## 231126 的默认库补充证据

调度详情 detail.database = pdata_n；证据哈希：352009a820aec0f2561f77f3290e08c2d09d0edcf701d834c0eca5668a295d09。据此补齐 SQL 未限定库名。表目录记录 42330 的字段为 cust_no、cust_name、sex、inr_org_id、khjb、mobile、disturbConfig、org_name、branch_id、branch_name、total_net_asset、wxid、clientType、jggtkh、rzrqkh、gpqqkh、kyczt、birt_date、demise_flag、busi_date；没有已解析分区。它与任务 SQL 的材料字段结构冲突，不能按字段位置强行绑定。
