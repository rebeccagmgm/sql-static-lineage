# 物理对象目录：正式模型、加工中间对象与测试资产

[返回入口](00-20260911-认知实验/t06专题-区域/README.md)

查询日期：2026-09-14。范围为两个已选定目录中表名严格以 `t06_` 开头的记录，按各自身份统计；没有用任意字符串含 `t06` 的源表编号替代主题边界。

## 1. 元数据目录全部 T06 对象

| 完整表名 | 平台 | 注释 | 解析状态 | 已取得字段数 | 本专题角色 |
|---|---|---|---|---|---|
| `pdata_n.t06_admin_area` | hive | 行政区域 | PARSED | 21 | 正式行政区域模型 |
| `temp.t06_admin_area_mid_sip001` | hive | 空 | UNSUPPORTED | 0 | 比较新旧内容的中间对象 |
| `temp.t06_admin_area_temp_sip001` | hive | 空 | UNSUPPORTED | 0 | 标准化新源记录的中间对象 |

两个 TEMP 对象在元数据中为 UNSUPPORTED，字段未独立解析；其用途由主任务 SQL 证实，结构线索来自 CTAS，未伪装成已取得的独立 DDL。正式表为 20 个普通字段加来源分区字段，全部 string。

## 2. 平台目录全部 T06 资产

| 库名 | 表名 | 目录类型 | 范围 | 分类路径 |
|---|---|---|---|---|
| `bd_dm_rd_uat` | `t06_admin_area` | 003000 | 测试资产，结构与数据未套用正式表 | 本次未取得标签 |
| `pdata_n` | `t06_admin_area` | 003000 | 正式模型目录 | 分类_按元数据分类_其他_技术管理_运行管理_配置及参数信息；分级_1；自定义_数据重要性_核心；自定义_模型主题_地址；自定义_资讯_模型表 |
| `pdata_n_uat` | `t06_admin_area` | 003000 | 测试资产，结构与数据未套用正式表 | 本次未取得标签 |

平台目录整体状态为 PARTIAL。上面 3 个 GUID 与元数据目录 3 个物理对象是不同统计集合，存在重合且覆盖不同，不能相加。正式 Hive 对象按元数据来源与平台完整物理名称对应；测试对象未继承正式字段和注释。

## 3. 正式表的原始字段定义

| 顺序（从 1 起） | 字段 | 类型 | 注释 | 是否分区 |
|---|---|---|---|---|
| 1 | `addr_id` | string | 地址编号 | 否 |
| 2 | `admin_area_name` | string | 行政区域名称 | 否 |
| 3 | `admin_area_full_name` | string | 行政区域全称 | 否 |
| 4 | `prov_shor_name` | string | 所属省份简称 | 否 |
| 5 | `prov_id` | string | 所在省份 | 否 |
| 6 | `city_id` | string | 所在地市 | 否 |
| 7 | `zip_code` | string | 邮编 | 否 |
| 8 | `area_cd` | string | 区号 | 否 |
| 9 | `admin_area_lvl_cd` | string | 行政区域层级代码 | 否 |
| 10 | `upper_addr_id` | string | 上级行政区域编号 | 否 |
| 11 | `cptl_name` | string | 驻地 | 否 |
| 12 | `popu` | string | 人口万人 | 否 |
| 13 | `area_size` | string | 面积平方千米 | 否 |
| 14 | `del_flag` | string | 删除标志 | 否 |
| 15 | `del_date` | string | 删除日期 | 否 |
| 16 | `data_src_cd` | string | 数据来源代码 | 否 |
| 17 | `task_name` | string | 任务名 | 否 |
| 18 | `data_etl_date` | string | 数据加载日期 | 否 |
| 19 | `data_upt_date` | string | 数据更新日期 | 否 |
| 20 | `data_time` | string | 数据时间 | 否 |
| 21 | `src_tbl` | string | 源表 | 是 |

字段的业务解释、候选键和使用限制见[字段与身份](00-20260911-认知实验/t06专题-区域/建模基础/字段与身份.md)，不把 DDL 字段列表当成已证明的主外键合同。

## 4. 与 T06 有直接关系的其他表

| 表 | 已取得结构 | 在本专题的角色 |
|---|---|---|
| `dm_hr_n.hssext_sync_prov_city_relationship` | 26 字段（含分区） | 区域资料同步目标结构，实际到达未核验 |
| `odata_n_sip.f_webdata_rpa_xzqh_code` | 10 字段（含分区） | RPA 行政区域来源，按业务日期分区 |
| `odata_n_uip.d_t_admin_division_code` | 13 字段（含分区） | 资讯消费侧补充编号和层级的来源 |
| `pdata_news_n.tyzx_admin_area` | 13 字段（含分区） | 资讯派生区域，按业务日期分区 |

这些是来源或消费者，未计为 T06 前缀业务模型。手机号归属草稿另见[04](00-20260911-认知实验/t06专题-区域/04-手机号归属区域.md)，未计入正式物理清单。

原始记录：[物理对象与字段 JSON](attachments/物理对象目录/IMG-物理对象目录-20260914105650275.json)、[平台分类快照](attachments/00-全貌与范围/IMG-00-全貌与范围-20260914105651214.json)、[相关表字段 JSON](attachments/README/IMG-README-20260914105650256.json)。物理身份、快照时间、源路径与哈希保留在 JSON，表名相同不自动合并。
